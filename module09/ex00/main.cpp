#include "BitcoinExchange.hpp"

int main(int argc, char **argv)
{
    if (argc != 2)
        throw (std::out_of_range("Only 1 argument allowed"));
    (void)argv;
    parseDB();
    /*try to print the mapppppppppp and then parse the input file
    */
    return(0);
}